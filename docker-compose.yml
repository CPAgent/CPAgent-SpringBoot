services:
  db:
    image: postgres:18-alpine
    container_name: cpagent_postgres
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  pgdata:
    name: cpagent_pgdata
